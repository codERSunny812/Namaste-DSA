const arr = [3,1,2,0];


const originalSum = (arr.length* (arr.length +1))/2;

const ArrayElementSum = calculateSum(arr);

function calculateSum(arr){
    let sum = 0;

    for(let i=0;i<arr.length;i++){
        sun += arr[i];

    }


    return sum;
}



return originalSum -ArrayElementSum;